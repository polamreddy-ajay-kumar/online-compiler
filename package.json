{
  "name": "online-compiler",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based website builder and Docker sandbox compiler",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
