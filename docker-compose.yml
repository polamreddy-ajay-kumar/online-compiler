services:
  online-compiler:
    build: .
    container_name: online-compiler
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
